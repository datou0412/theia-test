{
  "name": "root",
  "private": true,
  "scripts": {
    "prepare": "lerna run prepare",
    "start": "cd browser-app && yarn theia start --plugins=local-dir:../plugins"
  },
  "workspaces": [
    "packages/*",
    "browser-app"
  ],
  "devDependencies": {
    "lerna": "^3.13.4"
  }
}